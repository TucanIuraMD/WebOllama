/* Login page — handled by app.js, this is a stub */
(function () {
  // login / logout is handled in app.js — nothing to render here
  window.Pages = window.Pages || {};
  window.Pages.login = { render: (el) => { el.innerHTML = ""; } };
})();